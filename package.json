{
  "name": "estacio-agent",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "render-build": "PUPPETEER_CACHE_DIR=/opt/render/project/src/.puppeteer npm install && npx puppeteer browsers install chrome --cache-dir=/opt/render/project/src/.puppeteer",
    "postinstall": "npx puppeteer browsers install chrome --cache-dir=/opt/render/project/src/.puppeteer || npx puppeteer browsers install chrome --cache-dir=$(pwd)/.puppeteer"
  },
  "dependencies": {
    "puppeteer": "^23.0.0",
    "node-cron": "^3.0.2",
    "dotenv": "^16.0.0"
  }
}
