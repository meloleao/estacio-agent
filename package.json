{
  "name": "estacio-agent",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "postinstall": "puppeteer browsers install chrome",
    "render-build": "PUPPETEER_CACHE_DIR=/opt/render/.cache/puppeteer npm install && npx puppeteer browsers install chrome --cache-dir=/opt/render/.cache/puppeteer"
  },
  "dependencies": {
    "puppeteer": "^23.0.0",
    "node-cron": "^3.0.2",
    "dotenv": "^16.0.0"
  }
}
